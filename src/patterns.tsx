import React from "react";
import MainWrapper from "./wrapper/main-wrapper";

export const Patterns = ({ children }) => {
  return (
    <MainWrapper>
      <div></div>
    </MainWrapper>
  );
};

export default Patterns;
